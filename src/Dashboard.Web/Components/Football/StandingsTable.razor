@* Wiederverwendbare Ligatabelle (Top-5, CL-Ligaphase, Turnier-Gruppen). Getrackte Vereine
   (Table.Rows[i].IsOwnTeam) werden hervorgehoben. *@
<table class="league-table @(MarkRelegation ? "has-relegation" : null)">
    <thead>
        <tr>
            <th class="num">#</th>
            <th>Verein</th>
            <th class="num">Sp</th>
            <th class="num">S</th>
            <th class="num">U</th>
            <th class="num">N</th>
            <th class="num">Diff</th>
            <th class="num">Pkt</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var row in Table.Rows)
        {
            <tr class="@(row.IsOwnTeam ? "is-own" : null)">
                <td class="num">@row.Position</td>
                <td>
                    <span class="team-cell">
                        <CrestMark Url="@row.CrestUrl" ShortCode="@row.ShortCode" Name="@row.TeamName" />
                        <span class="team-name">@row.TeamName</span>
                    </span>
                </td>
                <td class="num">@row.PlayedGames</td>
                <td class="num">@row.Won</td>
                <td class="num">@row.Draw</td>
                <td class="num">@row.Lost</td>
                <td class="num">@(row.GoalDifference > 0 ? "+" : "")@row.GoalDifference</td>
                <td class="num pts">@row.Points</td>
            </tr>
        }
    </tbody>
</table>

@code {
    [Parameter, EditorRequired] public LeagueTable Table { get; set; } = default!;

    /// <summary>Die beiden letzten Plätze als Abstiegsplätze markieren. Nur für echte Ligatabellen —
    /// in einer Vierergruppe eines Turniers gibt es keinen Abstieg, deshalb entscheidet der Aufrufer.</summary>
    [Parameter] public bool MarkRelegation { get; set; }
}
